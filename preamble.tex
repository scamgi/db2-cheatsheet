% Preamble for the Databases 2 Cheatsheet

% Set the document class to article, A4 paper, and landscape orientation.
\documentclass[a4paper, landscape]{article}

% --- PACKAGE IMPORTS ---

% Encoding
\usepackage[utf8]{inputenc}

% Geometry package to set margins and layout
% We set a small margin to maximize space on the cheatsheet
\usepackage[a4paper, landscape, margin=1cm]{geometry}

% Package for creating multiple columns
\usepackage{multicol}

% Advanced math formulas
\usepackage{amsmath}

% For placeholder text (can be removed later)
\usepackage{lipsum}

\usepackage{xcolor}
\usepackage{fancyvrb} % Use the fancyvrb package
\usepackage{listings}
% Define a background color for code blocks
\definecolor{codegray}{rgb}{0.95,0.95,0.95}

% Define the style for the code listings
\lstset{
    language=Java,
    backgroundcolor=\color{codegray},
    basicstyle=\ttfamily\small,
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=5pt,
    breaklines=true,
    breakatwhitespace=false,
    showstringspaces=false,
    frame=none,
    commentstyle=\color{green!60!black},
    keywordstyle=\color{blue},
    stringstyle=\color{purple},
    % Add JPA annotations as keywords for better highlighting
    morekeywords={Entity,Id,GeneratedValue,OneToOne,OneToMany,ManyToOne,ManyToMany,JoinColumn,JoinTable,NamedQuery,Temporal,OrderBy,ElementCollection,CollectionTable,MapKeyJoinColumn,Column,Embeddable,EmbeddedId,MapsId}
}


% For better font handling
\usepackage[T1]{fontenc}

% for the graphs
\usepackage{tikz}
\usetikzlibrary{arrows.meta}

% --- DOCUMENT METADATA ---

\title{Databases 2 Cheatsheet}
\author{Giacomo Scampini}
\date{\today}

% --- CUSTOM COMMANDS & SETTINGS ---

% Set the paragraph indentation to zero, and add a small vertical space between paragraphs.
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em}
\setlength{\columnsep}{1cm} 

% Define the style for sections to be more compact
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}

% Ensure page numbers are on subsequent pages
\pagestyle{plain}
